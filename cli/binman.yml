- name: ripgrep
  linux:
    x64:
      url: https://github.com/BurntSushi/ripgrep/releases/download/15.1.0/ripgrep-15.1.0-x86_64-unknown-linux-musl.tar.gz
      path: "optional if defined to output if valiud path resolve to it else add the basepath/bin to it"
      sha256: "either be sha:string or just string"
      pattern: "pattern to find the binary files want"
  windows:
    x64:
      url: https://github.com/BurntSushi/ripgrep/releases/download/15.1.0/ripgrep-15.1.0-x86_64-unknown-linux-musl.tar.gz
      path: "optional if defined to output the download content to"
      sha256: "either be sha:string or just string"
      pattern: "pattern to find the binary files want"
